MONGODB_URI=mongodb://127.0.0.1:27018
MONGODB_DB=receipt_issuer
# Platform administrators are created with `npm run admin:create`, not through
# an environment variable. These scripts read this file directly, so there is
# nothing to export into the shell.
